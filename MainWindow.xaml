<Window x:Class="WpfApp2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp2"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="600">
    <Grid x:Name="grid">
        <Grid.RowDefinitions>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width ="*" />
            <ColumnDefinition Width ="*" />
            <ColumnDefinition Width ="*" />
        </Grid.ColumnDefinitions>
        <Canvas x:Name ="canvas1" Height = "80" Width = "80" Background="Blue" VerticalAlignment="Bottom" Margin="50,0,0,50" 
                KeyDown="Canvas_PreviewKeyDown" Focusable="True" Grid.Column="0"/>
        <Canvas x:Name = "canvas" Height = "50" Width = "50" Grid.Column="1" />
        
    </Grid>
    
</Window>
